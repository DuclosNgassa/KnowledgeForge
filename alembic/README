Generic single-database configuration.

## To run a migration
```Powershell
uv run alembic revision --autogenerate -m "YOUR UPDATE SCHEMA MESSAGE"
```

Then inspect the generated migration before running:
```Powershell
uv run alembic upgrade head
```